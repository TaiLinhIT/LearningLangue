<Project>
  <PropertyGroup>
    <!-- Keep solution builds sequential on machines where parallel MSBuild graph evaluation is unstable. -->
    <BuildInParallel>false</BuildInParallel>
  </PropertyGroup>
</Project>
